import BrowserClient from './browser';

describe('BrowserClient', () => {

  describe('health', () => {
    it('When the target is working, should resolve to true', () => {
      console.log('dasda');
    });
  });
});
